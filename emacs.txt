#-------CREATE SCHEME REPL------------------
# 1. install mit scheme, it may be needed
#    to put scheme on the path M-x custimize-group; scheme; and
#    change "scheme program name" setting into name of scheme interpreter
#    (may be mit-scheme or smth else, default is scheme)
# 2. M-x run-scheme
# 3. C-c C-e, C-x C-e for last definition and last expression respectively

#-------SPELL CHECKING ----------------------
M-x ispell-buffer
